server:
  port: 9000
spring:
  application:
    name: mymall-member
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.88.88:3306/mymall_ums?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      server-addr: localhost:8848
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto #  主件自增

